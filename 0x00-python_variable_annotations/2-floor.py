#!/usr/bin/env python3

"""
Module: 2-floor
Contains a type-annotated function `floor` that takes a float as an argument
and returns its floor as an integer.
"""

import math


def floor(n: float) -> int:
    """
    Function: floor
    Takes a float as an argument and returns its floor as an integer.
    """
    return math.floor(n)
